# TeamBot PRD v3 — 以「怎麼用才方便」重寫

日期:2026-09-24。狀態:核心端到端已跑通(27 task / 測試 136 passed)。
v2 在 git 歷史 `20c4404..7ea1bc6`。參考:`docs/architecture.md`、`docs/product-brainstorm.md`;體驗證據見附錄 A(讀 telecodex 原始碼)。

---

## 前言:這版跟 v2 差在哪

v2 是**功能表**——把 telecodex 有的東西逐條抄成需求 H~O,再標「已實作/規劃」。問題是:功能齊全不等於好用。一個功能全開但每分鐘推播一次的 bot,在手機上活不過三天。

v3 改成從**使用時刻**往回推:先問「這一刻使用者在做什麼、痛在哪」,再決定要什麼功能。推導出來的結果和 v2 的優先序**明顯不同**(§7)。

兩個實質轉向:

1. **推播經濟學成為骨架**(§0/§5)——「什麼時候可以吵你」不是細節,它決定這產品在手機上活不活得下去。
2. **安全從「問你」改成「硬牆 + 名單」**(§6)——v2 是「危險操作一律要問」,v3 是路徑越界**直接拒**、危險指令**直接拒**、日常指令**直接跑**,只有灰區才問。**批准疲勞本身就是安全漏洞**:問太多次,人就會閉著眼按 `ok`。

**刪掉的東西**(v2 有、v3 拿掉):`run <專案>`、`sessions`、`use latest`、`new`(§4);多會話、歷史匯入、ACL 角色、設定檔驅動全部降級(§7)。理由一致:**選項愈多愈不方便**。

---

## 0. 整份設計壓在一個假設上(P0,必須先驗證)

> **Teams 中「編輯既有訊息」不會發手機推播,「新訊息」會。**

如果這成立,整個體驗設計就有了骨架:**進度用編輯(安靜)、要你做事才用新訊息(響)**。每輪任務的推播預算可以壓到常態 1 次(§5)。

如果不成立(編輯也推播),那麼「長任務持續回報」和「不吵你」就是直接衝突,必須改用完全不同的策略——例如進度只存在 `!tb status` 拉取、完全不主動推。**這會推翻 §3.2 和 §5 的全部設計。**

### 驗證方法(尚未做)

1. 常駐 runner 發一則訊息到自己聊天 → 手機確認收到推播。
2. 同一則訊息連續編輯 3 次 → 手機觀察**是否再次推播**、以及鎖定畫面預覽是否更新。
3. 記錄於 `tasks/taskNNN-notification-economics/evidence/`。

在這件事驗證前,§3.2 不應開工。這是本版最重要的一條。

---

## 1. 它到底解決什麼(一個具體的下午)

你在外面。想起早上那個測試還紅著。

- **14:02** 你打開 Teams 的「與自己聊天」,語音講一句:「把昨天那個型別錯誤修掉,跑一下測試」。
- **14:02** 幾秒內出現一則 `⏳ 已收到`。你把手機收起來。
- **14:03–14:13** 它跑 `git diff`、改檔、跑 `npm test`——**全在白名單,一次都沒吵你**。你在咖啡廳順手瞄一眼,那則訊息已經變成 `⏳ npm test 執行中 · 更新於 14:07:32`。
- **14:14** 手機**響第一次也是唯一一次**:「✅ 完成 · 改 1 檔 · 12 passed」。
- **19:30** 你回到電腦,在 `D:\AgentHub` 開終端機 `codex resume`——**接的是同一個 thread**,下午講過的話不用重講。

如果它中途想跑一個沒見過的指令(例如 `npx some-codemod`),才會多響一次問你,你回 `ok`,或回 `ok 永遠` 讓它以後別再問。如果它想動 `D:\AgentHub` 以外的檔,**連問都不會問,直接拒絕**(§6.1)。

這份 PRD 的全部目的,就是讓上面這 5 個時刻都不卡。

---

## 2. 六條設計原則

| # | 原則 | 具體含意 |
|---|---|---|
| P1 | **打字量與風險成正比** | 安全的事 0~1 個字;只有危險、只有真的會搞砸的事,才要求你多打字確認 |
| P2 | **編輯優於新訊息** | 新訊息是稀缺資源(會推播)。進度一律就地編輯 |
| P3 | **沉默也是訊號** | 不刷屏,但**永遠不無聲卡住**;心跳讓你知道它還活著 |
| P4 | **預設就對** | 不指定專案 = AgentHub;不指定參數 = 最合理的值。幾乎不需要設定 |
| P5 | **結論優先,細節 on demand** | 手機預設只給一行;`!tb 細節` 才展開 |
| P6 | **安全邊界是硬牆,不是問答題** | 「絕對不行」的事直接拒(不問)、「絕對可以」的事直接跑(不問);只有灰區才打擾你(§6) |

---

## 3. 六個使用時刻

每個時刻寫四件事:**現在的痛 → 設計 → 為什麼這樣方便 → 怎麼驗收**。

### 3.1 交辦的那 5 秒

**痛**:手機打字慢。要先想「這屬於哪個專案」「指令怎麼拼」更慢。

**設計**
- `!tb <自然語言>` 就是交辦,**不需要專案名**,預設 AgentHub。(已實作)
- 前綴接受 `!tb` / `@tb` / 全形 `！tb` / `＠tb`。(已實作)
- **自己聊天可關前綴**:那個對話裡每則訊息都是給它的,要求打前綴是純粹的稅。以 `.` 開頭 = 給自己的筆記,不送 Codex。→ **新增需求,見 §7 R2**
- **語音交辦**:Teams 手機端按住就能錄。轉逐字後併入 prompt。→ **§7 R3(從 v2 的「選用」升到 P1)**
- **分享即交辦**:從瀏覽器/相簿分享到 Teams 自己聊天,附件落 `.teambot/inbox`,連同文字一起交辦。→ **§7 R4**

**為什麼方便**:把交辦成本從「解鎖 → 找對話 → 打前綴 → 想專案名 → 打完整句子」壓到「按住講一句」。

**驗收**:交辦一件事的中位輸入成本 ≤ 15 字元,或 1 則語音。

---

### 3.2 等待的那 10 分鐘 ⚠️ 依賴 §0 驗證

**痛**(兩個相反方向,這才是真正難的地方):
- 太安靜 → 「它是不是掛了?」焦慮到想跑回電腦。
- 太吵 → 手機每 20 秒震一次,第二天就把通知關掉,等於整個產品死掉。

**設計**
- 一輪只佔**一則訊息**,內容持續被覆寫(⏳ → 正在做什麼 → 結論),**用編輯,不發新訊息**。
- 更新節流:`max(900ms, 送出間隔)`,且內容沒變就不送。
- 訊息裡帶**心跳時間戳**(`更新於 14:07:32`)。這是我們版本的「正在輸入」——Teams 網頁 DOM 不適合做 typing 指示器,但一個會動的時間戳同樣能回答「它還活著嗎」。
- 進行中只顯示**截斷後的第一段**;收尾才給全文。
- **卡住要說**:超過 N 分鐘沒有任何 Codex 事件 → 訊息改成 `⚠️ 已 8 分鐘無進展`,而不是繼續假裝在跑。

**為什麼方便**:你瞄一眼就知道進度,而它從頭到尾**沒有吵過你一次**。焦慮和噪音同時解決。

**驗收**:長任務期間手機推播 **0 次**;任何時刻「不確定它還活著」的時間為 0(心跳間隔 ≤ 60s)。

---

### 3.3 被打斷的那一刻(決策關卡)

**痛**:telecodex 按一顆按鈕就好。我們走 CDP 操作網頁版,**送不出可互動卡片**——這是硬限制。若照搬「打確認碼」,使用者要在手機上準確打出 `!tb approve a3f2`,錯一個字元就失敗。這是目前體驗最差的一環。

**最重要的一步是讓這一刻很少發生。** 經 §6 的硬牆 + 名單制之後,日常開發指令(`npm test`、`git diff`)**根本不會問你**,危險指令**直接拒也不問**——真正需要你決定的只剩「沒見過的灰區指令」。先把數量壓下來,再優化單次成本:

**設計(按打字量由少到多)**

| 情境 | 使用者要打 | 說明 |
|---|---|---|
| **只有一筆待決**(絕大多數) | `ok` / `好` / `y` / `no` | **不需要代碼**。省略時就是指唯一那筆 → **§7 R1,最高 CP 值** |
| 引用回覆該批准訊息 | `ok` | 從引用來源反查 requestId(Teams DOM 可行性**待驗證**) |
| 多筆待決 | `ok 2` | 代碼用**每對話遞增的小序號 1/2/3**,不用隨機碼——手機上好打、好念、不會看錯 |
| 這類以後都別再問 | `ok 永遠` | 把該指令樣式加進**該專案白名單**(§6),下次直接跑 |

- 批准訊息**必須是新訊息**(要推播),且**一次只問一個問題**。
- 等待期間,狀態卡同步改成「⏸ 等你批准 `npm test`」——讓你看得出**為什麼停住**,而不是以為它在跑。
- 逾時 15 分鐘 → **自動拒絕**,永不自動批准。逾時後訊息改成「已逾時拒絕,要重試回 `retry`」。

**為什麼方便**:兩層同時壓——**次數**靠 §6 名單制壓到接近 0,**單次成本**從「11 個字元且不能打錯」壓到「2 個字元」。而 `ok 永遠` 讓打擾隨使用時間**遞減**。

**驗收**:一般開發任務全程 **0 次批准**;真要問時 ≤ 3 字元;批准訊息永遠指明「在等什麼」。

---

### 3.4 拿回結果

**痛**:手機上看 diff 是酷刑。倒一大段 log 出來等於沒回報。

**設計**
- **兩段式**:第一行就是結論(`✅ 完成 · 改 1 檔 · 12 passed`),細節接在後面但**預設摺疊感**(短)。
- 結果是**新訊息**(要推播),同時把那則進度卡編輯成一行收掉,避免兩則訊息打架。
- 取權威結果只認 `final_answer`,忽略 commentary/deltas;一個 turn **冪等只回一次**。(已實作)
- 細節 on demand:`!tb 細節` / `!tb diff` / `!tb 檔案`。
- 產物(`turns/out` 下的檔案)**自動回傳**成 Teams 附件,不用你再去翻路徑。→ **§7 R4**

**為什麼方便**:鎖定畫面掃一眼就知道成不成;要細節時才花流量和注意力。

**驗收**:結論在**第一行 30 字內**可判斷成敗。

---

### 3.5 回到電腦前(這其實是最大的賣點,v2 完全沒講)

**痛**:多數「手機遙控」工具的上下文是斷的——手機上聊完,回電腦得重講一遍。

**設計**
- 因為是**同一個常駐 Codex thread** 跑在 `D:\AgentHub`,回到電腦開終端機 `codex resume` 接的就是同一段對話。**這件事現在已經成立**,只是從來沒被當成功能寫出來。
- `!tb 交接` 印出 thread id + cwd + 這輪改了哪些檔,方便直接接手。→ **§7 R5**
- 反向也成立:電腦上做到一半出門,手機接著問。

**為什麼方便**:手機不是一個「次等的遙控器」,它和你的終端機是**同一個工作階段的兩個視窗**。0 次重述上下文。

**驗收**:手機交辦 → 電腦 `codex resume` → 追問「剛剛那個檔」,它答得出來。

---

### 3.6 搞砸的時候

**痛**:誤發、發錯、跑歪了、它開始做你沒要它做的事。

**設計**
- **最壞情況被上限鎖死**:不管交辦內容多離譜,它**出不了 `D:\AgentHub`**(§6.1 硬牆),也**跑不了黑名單指令**(§6.2)。所以誤發的損害有明確天花板——這是「敢隨手交辦」的真正來源。
- **預設 read-only**,誤發的最壞後果是「浪費幾秒讀了些檔」。
- 要寫入得 `!tb unlock 30`(時限)。**時限會自己到期回到安全狀態**——你忘記 lock 不會變成長期風險。注意 `unlock` **只開放 AgentHub 內的寫入,不會擴大路徑範圍**。
- 跑歪了:執行中直接送純文字 = `turn/steer` 併入當前輪,不用先 stop 再重下。
- 停:`!tb stop`(收尾停)/ `!tb kill`(硬停)。
- 沒登入 Codex 時**不要靜默失敗**,直接說「Codex 未登入」並給 `!tb login`。→ **§7 R6**

**為什麼方便**:錯誤的代價被設計成很低,所以你敢隨手交辦——**敢用**才是便利的前提。

**驗收**:任何誤發在 read-only 下都不會改到檔案;`unlock` 到期自動回 read-only;**針對 AgentHub 外路徑的存取嘗試(含 `..`、UNC、symlink 繞道)全部被拒且留下稽核**。

---

## 4. 指令表(按實際常用度排,不按功能分類)

| 你會打的 | 意思 | 狀態 |
|---|---|---|
| `!tb <一句話>` | 交辦,預設 AgentHub | ✅ |
| `ok` / `no` | 批准/拒絕**唯一**待決 | 🔜 R1 |
| (直接打字,執行中) | 中途轉向 `turn/steer` | 引擎 ✅、指令待接 |
| `!tb status` | 拉快照 | ✅ |
| `!tb 細節` / `diff` / `檔案` | 展開細節 | 部分 |
| `!tb stop` / `kill` | 收尾停 / 硬停 | ✅ |
| `!tb unlock 30` / `lock` | 開放 **AgentHub 內**寫入 30 分鐘 / 立刻收回(**不擴大路徑範圍**,§6.1) | ✅ |
| `ok 2` / `no 2` | 多筆待決時指定序號 | ✅(改小序號 🔜) |
| `ok 永遠` | 該指令樣式加進本專案白名單,以後不問 | 🔜 R7 |
| `!tb 名單` | 看目前白名單/黑名單 | 🔜 R7 |
| `!tb 交接` | 印 thread/cwd,回電腦接手 | 🔜 R5 |
| `!tb login` / `logout` | Codex 裝置登入 | 🔜 R6 |
| `!tb help` | 說明 | ✅ |

**刻意不做**:`run <專案>`、`sessions`、`use latest`、`new` 這些在單人 + 單一 AgentHub 的場景下是**多餘的選擇**,只會讓人猶豫。留到真的有多專案需求再開(§7 P2)。

---

## 5. 推播預算(一頁講完什麼時候會吵你)

每輪任務目標:**常態 1 次**(只有「完成」)。加上 §6 名單制之後,批准那次在一般開發任務裡不會發生,**≤2 次**是最壞情況而非常態。

| 事件 | 送法 | 推播? |
|---|---|---|
| 收到交辦 → `⏳ 已收到` | 新訊息 | 你剛發完,人在螢幕前,不算成本 |
| 進度更新(可能數十次) | **編輯同一則** | **否** ← 整份設計的關鍵 |
| 心跳時間戳 | **編輯同一則** | **否** |
| 指令命中**白名單**直接跑 | **編輯同一則** | **否** ← §6.3 買到的安靜 |
| 指令命中**黑名單/路徑越界**被拒 | **編輯同一則**(寫明拒絕原因) | **否**(不必問你,所以也不必吵你) |
| 需要你批准(僅**灰區**) | **新訊息** | **是**(少數情況才發生) |
| 完成 / 失敗 | **新訊息** | **是**(常態唯一一次) |
| 卡住警告(>N 分鐘) | 編輯 + **一次**新訊息 | 是(例外,值得) |

> 前提是 §0 的假設成立。**這張表就是 §0 為什麼是 P0。**

---

## 6. 安全模型:兩道硬牆 + 一組名單(而不是一直問你)

**設計出發點是便利,不是防禦。** 批准疲勞是真實的失敗模式:問太多次,人就會閉著眼按 `ok`,那等於沒有安全。所以把「絕對不行」做成**硬牆**、「絕對可以」做成**白名單**,剩下要問的才會少到你願意認真看。

判斷順序(先命中先決定,**不往下走**):

```
① 路徑硬牆    → 碰到 AgentHub 外 ──────────► 拒絕,不問(連你都不能批准)
② 指令黑名單  → 命中 ──────────────────────► 拒絕,不問
③ 指令白名單  → 命中 ──────────────────────► 直接執行,不打擾
④ 以上皆非(灰區) ─────────────────────────► 才問你(§3.3)
```

### 6.1 硬牆一:路徑(不可批准)

**任何讀取或寫入,只要目標落在指定資料夾外,一律違規、直接拒絕。** 指定資料夾預設為 `D:\AgentHub`。

- 這**不是**批准題。即使是 owner 本人也不能當場放行——要擴大範圍只能改設定並重啟,不能在對話中被說服。
- 涵蓋繞道寫法:`..` 相對路徑跳脫、絕對路徑、UNC(`\\server\share`)、symlink/junction 指向外部、`%USERPROFILE%` 之類環境變數展開後越界。一律以**解析後的真實路徑**判定。
- 為什麼這樣更好用:這是唯一能讓你**敢隨手交辦**的保證。你不必在批准當下自己判斷「這個路徑安不安全」——那本來就不該是手機上做的事。
- `unlock` 只切換「AgentHub **之內**能不能寫」,**永遠不擴大路徑範圍**。

### 6.2 硬牆二:指令黑名單(不可批准)

命中即拒,**不會拿來問你**——對 `rm -rf` 問一句「要嗎?」本身就是設計失敗。

| 類別 | 例(樣式比對,非窮舉) |
|---|---|
| 破壞性刪除/格式化 | `rm -rf`、`del /s /q`、`Remove-Item -Recurse -Force`、`format`、`diskpart` |
| Git 歷史改寫 | `push --force`、`reset --hard`、`clean -xfd`、`filter-branch` |
| 提權 | `sudo`、`runas`、`Start-Process -Verb RunAs`、`net user` |
| 機密外洩 | 讀取 `~/.ssh`、`*.pem`、`.env`、`credentials`、`GH_TOKEN` 後外送 |
| 下載即執行 | `curl … \| bash`、`iwr … \| iex`、`Invoke-Expression` |
| 削弱防護 | 關閉 Defender/防火牆、改安全性 registry、`Set-ExecutionPolicy Unrestricted` |
| 對外網路 | 預設整個沙箱 `networkAccess = false` |

### 6.3 白名單:日常指令直接跑(0 打擾)

| 類別 | 例 |
|---|---|
| 讀取/檢視 | `ls`、`dir`、`type`、`cat`、`git status`、`git diff`、`git log` |
| 建置測試 | `npm test`、`npm run <script>`、`npm ci`、`node`、`tsc`、`dotnet build`、`dotnet test` |
| 本地版控 | `git add`、`git commit`、`git checkout -b`、`git stash` |
| 檔案編輯 | AgentHub **之內**的建立/改寫(已受 §6.1 保護) |

- 白名單是 **per-project** 的,可由 `ok 永遠` 逐步長出來(§3.3)→ 打擾隨時間遞減。
- 白名單**不能覆蓋硬牆**:`git commit` 在白名單上,但若它要寫的檔在 AgentHub 外,仍然被 §6.1 拒絕。

### 6.4 其餘不可退讓

永不自動批准(灰區逾時 → 拒絕);`kill` 隨時可用;`unlock` 有時限且到期自動回 read-only;對外訊息與日誌去識別(token/cookie/env);owner 以不可變 sender id 授權;附加式稽核記錄每一次「拒絕/放行/加白名單」。

### 6.5 與現有實作的落差

`src/security/guards.ts` 目前把破壞性/網路/機密/提權歸類為 **always-approve(一律要問)**。依本節要改成 **deny(直接拒、不問)**,並新增可設定的白名單比對。這是行為變更,見 §7 **R7**。

**條款風險**:走 CDP 操作 Teams 網頁版有自動化條款風險,使用者需知情(`docs/architecture.md` §10)。

---

## 7. 用便利重排之後,roadmap 優先序變了

這是 v3 相對 v2 最實質的產出。同一批候選功能,換個評分標準就換了順序:

| 代號 | 需求 | v2 位置 | v3 | 為什麼變 |
|---|---|---|---|---|
| **R0** | **驗證推播經濟學**(§0) | 沒有這項 | **P0** | 它決定 §3.2/§5 成不成立,不驗證就開工等於賭 |
| **R1** | **單一待決時 `ok` 免代碼** | 沒有這項 | **P0** | 改動極小,直接解掉最痛的一步(11 字元 → 2 字元) |
| **R7** | **硬牆 + 名單制**(§6):路徑越界直接拒、指令黑名單直接拒、白名單直接跑、`ok 永遠` | v2 是「危險操作一律要問」 | **P0** | 比 R1 更根本:R1 降低**單次**成本,R7 降低**次數**。一般任務可做到 0 次批准 |
| **H** | 串流就地編輯接上 Teams | roadmap 第 1 項 | **P0** | 升級理由變了:它不只是「好看」,它是**唯一能不吵你的手段** |
| **R2** | 自己聊天免前綴(`.` 逃脫) | 沒有這項 | **P1** | 純粹省稅;但需一次性確認,避免把筆記誤送 |
| **R3** | 語音轉文字(原 M) | 標「**選用**」 | **P1** | 手機打字是最大摩擦。這是手機場景的第一輸入法,不是選配 |
| **R4** | 附件 in/out + 產物自動回傳(原 J) | roadmap 中段 | **P1** | 「分享即交辦」是最自然的入口;產物不自動回傳等於沒完成 |
| **R6** | Codex 裝置登入(原 L) | roadmap 中段 | **P1** | 未登入時**靜默失敗**是最糟的體驗,必須早做 |
| **R5** | `!tb 交接` | 沒有這項 | **P1** | §3.5 的賣點只差一個指令就能兌現,CP 值極高 |
| I | 多會話 / `use latest` / `sessions` | roadmap 第 2 項 | **P2 降級** | 單人 + 單一 AgentHub 用不到,反而增加選擇成本(§4) |
| K | 歷史匯入 | roadmap 中段 | **P2** | 常駐 resume 已覆蓋日常;匯入是邊緣情境 |
| N | ACL 角色 + audit_log | roadmap 中段 | **P2** | 單人用不到,等群組場景真的開了再說 |
| O | 設定檔驅動 | roadmap 末 | **P2** | 違反 P4「預設就對」。設定項愈多愈不方便 |

---

## 8. 現況 / 非目標 / 待決

**已完成並經真實驗證**(27 task,測試 136 passed 0 skipped):Phase 0 於真實租戶 GO(穩定 id、送出、接收、隱藏背景收訊);CDP 傳輸;常駐 Codex session + 重啟 `thread/resume` 連續;`!tb`/`@tb` + 預設 AgentHub;安全姿態(read-only / 時限 unlock / kill);結果彙整;狀態模型;批准註冊表(逾時自動拒);多任務 worktree(選用)。已整合進常駐 runner 並實測完整迴圈:**接收 → 執行 → 彙整回報 → 批准 → 重啟 resume**。

**非目標(首版)**:遷官方 Teams 傳輸(Graph / Bot Framework);跨組織多租戶;**AgentHub 以外的任何存取**(§6.1 硬牆,不是「暫未支援」而是**刻意不給**);團隊協作型 ACL。

**待決(需要你拍板)**
1. **自己聊天免前綴預設開還是關?**(R2)開了最方便,但若你把自己聊天當筆記本會誤送。建議:預設關,首次使用時問一次。
2. **卡住警告的 N 是幾分鐘?** 建議 5 分鐘。
3. **群組要不要做?** 目前所有設計都假設「自己聊天」。群組要加:前綴強制、批准碼不外曝、只回摘要。建議延後。
4. **`ok 永遠` 的白名單要不要能反悔?** 建議做 `!tb 名單 移除 <n>`,否則白名單只會長不會縮。
5. **讀取也受路徑硬牆限制嗎?** 建議「是」——否則 Codex 可以讀 `C:\Users\…\.ssh` 再把內容寫進 AgentHub 的檔案裡,硬牆等於漏了。代價是不能叫它「參考一下隔壁專案的寫法」。

---

## 9. 成功指標(全部可量測,取代 v2 的形容詞)

| 指標 | 目標 | 對應 |
|---|---|---|
| 交辦一件事的輸入成本 | ≤ 15 字元或 1 則語音 | §3.1 |
| **一般開發任務的批准次數** | **0**(靠名單制,不是靠放寬) | §6 |
| 真的要批准時的輸入成本 | ≤ 3 字元 | §3.3 |
| 每輪任務手機推播次數 | 常態 1,最壞 2 | §5 |
| 長任務期間「不知道它還活著」的時間 | 0(心跳 ≤ 60s) | §3.2 |
| 結論可判斷成敗所需閱讀量 | 第一行 30 字內 | §3.4 |
| 手機 → 電腦重述上下文次數 | 0 | §3.5 |
| 誤發造成的檔案變更 | 0(read-only 下) | §3.6 |
| **AgentHub 外的存取成功次數** | **0**(硬牆,含繞道寫法) | §6.1 |
| 自動批准次數 | 0(永遠) | §6.4 |

---

## 附錄 A:telecodex 的體驗證據

以下**讀 telecodex 原始碼確認**(非 README 推測),行號為 `D:\workspace\GitBank\GitRin\telecodex`。v3 的 §3.2 / §5 直接建立在這些觀察上。

| 觀察 | 出處 | v3 怎麼用 |
|---|---|---|
| `pending_text` 是**取代**不是追加,一輪只佔一則訊息 | `turns.rs:581-602` | §3.2 一則訊息持續覆寫 |
| `has_assistant_text` 一旦 latch,progress 不再覆蓋答案 | `turns.rs:584/596/605` | §3.4 最後一版永遠是結論 |
| 私訊寫進**草稿** `sendMessageDraft`,進度不落聊天記錄 | `turns.rs:47` | Teams 無此 API → 改用「編輯不推播」達成同樣效果(§0) |
| 節流 `max(edit_debounce_ms 900ms, 該 chat 送出間隔)` | `turns.rs:699-704` | §3.2 直接沿用 |
| 每 4 秒 `sendChatAction(typing)` 當存活訊號 | `turns.rs:1164-1184` | Teams DOM 不適用 → 改用**心跳時間戳**(§3.2) |
| `retry_after` → `edit_backoff_until`,期間跳過 flush | `turns.rs:623-635 / 799-807` | §3.2 限流時安靜降級 |
| `finish()` = `flush(force)`,略過 debounce/截斷,全文 `split_text` 分段 | `turns.rs:612-620 / 652-659` | §3.4 **過程可以掉,結論不能掉** |
| 進行中只送 `truncate_for_live_update`(第一段) | `turns.rs:810-818` | §3.2 進行中截斷 |
| 批准用 inline keyboard,逾時 **15 分鐘 → Decline** | `presentation.rs:27-92 / 187-197` | Teams 無按鈕 → §3.3 用 `ok` 免代碼補償;逾時策略照抄 |
| 狀態卡顯示「Waiting for command approval」 | `turns.rs:131` | §3.3 讓使用者看得出為何停住 |
| `/history` 用 Prev/Next **就地翻頁**不洗版 | `presentation.rs:94-118` | 同一精神 → §3.4 細節 on demand |

**傳輸差異**:telecodex 走 Telegram 官方 Bot API(long polling),乾淨合規且有草稿/按鈕/typing 三種原生手段。Teams 沒有等效的簡單個人 bot 通道,TeamBot 採 Electron 自有 WebContentsView + CDP,**失去了那三種手段**——§3.2/§3.3 的設計本質上就是在補這三個洞。

```text
手機 Teams ↔ Teams 雲端 ↔ Electron 自有 Teams 表面 ──CDP── TeamBot 常駐服務
                                                              ↕ stdio(app-server)
                                                        常駐 Codex 工作階段 @ D:\AgentHub
                                                              ↕
                                                    專案檔案 / 指令(受沙箱 + 批准約束)
```
